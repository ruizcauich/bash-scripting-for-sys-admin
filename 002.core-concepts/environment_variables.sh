#!/bin/bash

# we can use printenv or env to know what our variables are
# these contains all the current variables either local or global
# printenv
# env

# for local/bash session specific variables we can use 
# set

#  nothing to show since is system wide
# env | grep COLUMNS

# we can see the columns for the specific bash session
# set | grep COLUMNS


